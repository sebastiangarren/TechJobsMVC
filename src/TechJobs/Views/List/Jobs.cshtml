<h1>@ViewBag.title</h1>

<h3>@ViewBag.jobs.Count Result(s)</h3>


    @foreach (Dictionary<string, string> job in ViewBag.jobs)
    {

        foreach (KeyValuePair<string, string> column in job)
        {
            if (column.Key != "core competency")
            {
                <li>
                    @column.Value
                </li>
            }
            else
            {
                <li>
                    @column.Value
                </li>
                <p>***</p>
            }

        }
    }